<Window x:Class="Desktoper.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Desktoper.View"
        xmlns:ViewModels="clr-namespace:Desktoper.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="700">

    <Grid Background="#FFFFED7C">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Name="RightPanel">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            
            <Button Margin="0" Click="Button_Click"/>
            <Button Margin="0, 50, 0, 0" Click="Button_Click_1"/>
            <Button Margin="0, 100, 0, 0" Click="Button_Click_2"/>

            <Button Grid.Row="1" Background="red" Click="Button_Click_3">
                        
            </Button>
        </Grid>


        <local:AddingPrograms Grid.Column="1" Visibility="Hidden" x:Name="add"/>
        <local:ProgramListBox Grid.Column="1" Visibility="Visible" x:Name="prog"/>
        <local:ConsolePanel Grid.Column="1" Visibility="Hidden" x:Name="Cons" ClipToBounds="False"/>

    </Grid>
</Window>
